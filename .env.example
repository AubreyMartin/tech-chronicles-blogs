# Local development — run `npm run server` in another terminal
VITE_API_URL=http://localhost:3000

# Production (set this in Vercel → Settings → Environment Variables)
# VITE_API_URL=https://your-api.onrender.com
